# 为什么我让你这么执行

这个文档是"任务说明书"：把每一次你让我执行的任务，拆成
**执行内容 / 为什么这样执行 / 执行结果 / 0 基础解释** 四部分。
目的是让你（以及任何零基础的读者）明白两件事：

1. 每个任务**解决了什么问题**、**为什么用这种方式解决**而不是别的方式；
2. 整个系统是怎么一步步长成现在这样的。

建议按顺序读：任务之间有因果关系（比如"先做实验"才能"再决定上线"）。

---

## 任务 1：修复"英文问题偶尔返回中文答案"

**执行内容**

修改 `ask.py` 回答阶段的系统提示词（SYSTEM_PROMPT），把原来一句笼统的
"用和问题相同的语言回答"，改成明确的强制规则：
- 中文提问 → 必须用中文回答；
- 英文提问 → 必须用英文回答；
- 其他语言 → 尽量用相同语言回答。

同时加了 5 个自动化测试，用真实 DeepSeek 分别问了
"How long is the internship?" 和 "实习期是多久？" 验证。

**为什么这样执行**

- **问题根源**：大模型是"概率生成"的。提示词里"相同语言"这种模糊说法，
  模型有时理解为"用上下文里最多的语言"，而上下文（检索到的段落）几乎全是
  英文，所以它偶尔用英文回答中文问题、或反过来。模糊规则 → 模糊执行。
- **为什么改成三条硬规则**：把"语言的判断依据"从"模型自己猜"变成"看提问
  语言"这一条明确指令，并用 MUST（必须）这种强约束词。提示词工程的第一课：
  **想让模型稳定做什么，就把规则写得像法律条文，而不是像建议**。
- **为什么先写测试**：改提示词是"行为变化"，必须有一个自动化的检查来证明
  它有效，否则下次改代码可能悄悄退回旧行为。测试就是安全网。
- **为什么还要真实调用一次**：mock（模拟）测试只能证明"代码逻辑对"，
  不能证明"模型真的听提示词的话"。真实调用是最终验收。

**执行结果**

- 自动化测试 79 个全部通过（其中 5 个是本次新增）；
- 真实测试：英文问题返回英文答案，中文问题返回中文答案，来源引用正确；
- 这轮还顺带确认了一个关键设计：**中文问题虽然会先被改写成英文去检索，
  但生成答案时仍然用原始中文问题**——改写只影响"检索"，不影响"回答语言"。

**0 基础解释**

- **提示词（Prompt）**：你写给 AI 的指令文本。AI 没有"常识"，它的一切行为
  都来自指令和资料。
- **mock（模拟）**：测试时用假的对象替换真实依赖（比如假的公司接口），
  让测试又快又稳。真实调用则验证"假的不等于真的"。
- **为什么语言很重要**：如果用户问中文却收到英文答案，这个系统就像
  "会用但不会说话"——再准的检索也白费。

---

## 任务 2：切块策略离线对比实验（方案 A / B / C）

**执行内容**

在不修改正式数据库的前提下，用同一个 embedding 模型对比三种"文档切块"
策略，用 50 道评估题分别测试命中率：
- 方案 A：当前策略（500 字符窗口 + 自然断点 + 50 字符重叠）；
- 方案 B：按页面和自然段落切块；
- 方案 C：段落切块 + 相邻段落重叠。

每个方案用**临时**向量库，跑完就删，全程零 API 费用。

**为什么这样执行**

- **切块是什么**：向量检索的单位是"块"（chunk）。一份文档切成什么样的块，
  直接决定检索时能不能找到答案。这是整个 RAG 里"最值得先做实验"的一步，
  因为改切块要重新建库，返工成本最高。
- **为什么先实验再上线**：换策略是"伤筋动骨"的事（全部文档要重新切、
  重新向量化）。正确姿势是**先在临时环境证明它更好，再动正式环境**。
  这就是"离线实验"的意义：失败的成本只是一次脚本运行。
- **为什么三个方案一起比**：没有对比就没有结论。方案 A 是"现状"（对照组），
  B/C 是"候选"。只有 A 作为基线，才能看出 B/C 到底好在哪里、坏在哪里。
- **为什么用临时向量库**：绝不拿正式数据库做实验——实验可能写坏数据，
  而正式数据是服务的命根子。实验数据可重建，正式数据不能丢。
- **为什么加了 10 道"失败类型"题目**：评估题里已有的 40 题答得都不错，
  但"实习生向谁汇报""职位名称"这类题一直失败。**评估集要覆盖你最担心的
  场景**，否则"整体命中率提高了"会掩盖"最难的题还是不会"。

**执行结果**

- 方案 C（段落 + 相邻段重叠）胜出：Top-1 命中率 56% → 62%，Top-3 从 68% → 80%；
- 同时发现 6 道"专名/职位"类问题三个方案都救不了——这是 embedding 模型
  能力边界，不是切块问题（后续任务 6/7/8 继续解决）；
- 新增 25 个实验测试，全部 pytest 通过。

**0 基础解释**

- **chunk（块）**：文档被切成的一段段文本，是检索的最小单位。
- **为什么切块难**：切得太小，一个答案被劈成两半，谁也找不到；切得太大，
  一块里塞了太多无关内容，语义被稀释。方案 C 的思路是"按自然段落切，
  让相邻块共享一点内容"——段落是语义的天然边界，重叠防止答案刚好卡在
  两块中间。
- **Top-1 / Top-3 命中率**：问 100 道题，答案出现在返回的第 1 段里的比例
  （Top-1）；出现在前 3 段里的比例（Top-3）。Top-1 最严格，Top-3 更实用
  （因为答案生成时通常给模型前几段）。
- **离线实验**：不碰线上服务、不花真实费用的实验。它回答"值不值得上线"，
  而不是直接替你做决定。

---

## 任务 3：把方案 C 正式化 + 构建候选库 chroma_data_v2

**执行内容**

1. 把方案 C 的切块逻辑整理成正式函数 `load_document_paragraphs`，放进
   `chunker.py`（旧切块函数保留）；
2. 用方案 C 重建一个**候选向量库** `chroma_data_v2/`（15 块，旧库 10 块）；
3. 对候选库重跑 50 题评估，确认与离线实验完全一致；
4. 写 26 个边界测试（空文本、单段、多段、超长段、PDF 换页、元数据等）。

**为什么这样执行**

- **为什么实验函数要"转正"**：实验脚本里的代码是临时的，正式代码要有
  清晰的接口、注释和测试。把验证过的逻辑搬进正式模块，旧代码保留
  （兼容旧流程），这叫"平滑迁移"。
- **为什么叫"候选库"而不是直接替换**：正式库是服务的现状，不能"赌"。
  先建成一个**平行的新库**，和旧库并存。评估、冒烟都通过后，切换只是一个
  配置改动；万一有问题，切回去就是。
- **为什么保留 source/page**：这是整个项目从第一天就坚持的规矩——
  每个块必须记住"来自哪个文件、第几页"。没有它，答案就无法标注出处，
  也就没有"带出处的答案"这个核心功能。
- **为什么写边界测试**：切块函数处理的输入千奇百怪（空文件、超长段落、
  换页……）。边界测试就是"把最刁钻的情况提前问一遍"，比上线后被用户
  撞见强一万倍。

**执行结果**

- 候选库 `chroma_data_v2/`：15 块（旧库 10 块原样保留）；
- 50 题评估结果与离线实验逐项一致（Top-1 62% / Top-3 80%）；
- 4 项真实冒烟（/search、/query 英中各一）全部通过。

**0 基础解释**

- **为什么重叠不会导致重复答案**：相邻块共享一小段内容，是为了防止答案
  正好"卡在两块的接缝处"。检索时只要命中其中一块就能找到答案，
  代价只是多存一点数据。
- **元数据（metadata）**：附着在数据上的说明信息，比如"这段来自
  handbook.pdf 第 3 页"。检索时它跟着走，答案就能写"出处"。
- **可回滚（rollback）**：任何变更都保留一条"退回原状"的路。这不是胆小，
  是工程上的保险——你永远不希望"改坏了却回不去"。

---

## 任务 4：正式切换方案 C 与 chroma_data_v2

**执行内容**

1. 上传流程（`/documents/upload` 和命令行 `ingest.py`）改用
   `load_document_paragraphs`（方案 C 切块）；
2. `store.py` 的 `DB_DIR` 指向 `chroma_data_v2/`（正式库）；
3. 旧库 `chroma_data/` 保留为回滚库；
4. 上传一个临时文档，走真实 uvicorn 验证 /health、/search、/query，
   确认新文档确实写进了 v2 而不是旧库，然后删除临时文档。

**为什么这样执行**

- **为什么连"上传"也要改**：切块不是只影响历史文档——**以后每上传一个新
  文档，都要用新策略切**。如果只换库不换切块入口，新旧文档的切块标准
  不一致，检索质量会乱。所以"换库"和"换切块"必须一起完成。
- **为什么要求"临时测试数据用完就删"**：验证流程需要真实上传，但不能
  污染正式库。上传一个内容独特、能唯一定位的临时文档，验证完按文件名
  删除——正式文档一个不碰。
- **为什么改 store.py 只改一行**：整个系统里"数据库在哪"只由
  `DB_DIR` 一个常量决定，这是当初设计时就把"数据库路径"收敛到一处的
  好处——切换 = 改一个值 + 重启。

**执行结果**

- 正式 API 使用 v2 库（15 块），新上传走方案 C 切块；
- 真实冒烟 11 项全过（含"确认写入 v2 而非旧库"）；
- 141 个测试全部通过；旧库 10 块原样保留。

**0 基础解释**

- **为什么"先删后加"**：同名文件重复上传时，先删旧块再入库——保证库里的
  永远是"这个文件的最新版本"，不会残留旧版本碎片。
- **重启服务**：改配置后要让正在运行的进程重新读代码/配置。uvicorn 支持
  热重载（开发时），但正式切换我们还是完整重启，确保状态干净。
- **这一轮的核心心法**：**"换"永远是"建新的 + 验证 + 切换 + 保留旧的"**，
  而不是"删旧的 + 建新的"。

---

## 任务 5：Git 初始化与第一个提交（版本管理检查点）

**执行内容**

1. 检查 `.gitignore`，补齐遗漏项（`.pytest_cache`、`eval_rewrite_cache.json`、
   新增的 `chroma_data_v2`、`.claude`）；
2. 扫描所有待提交文件，确认没有任何 API key；
3. 创建第一个本地 commit：`feat: enable paragraph-overlap chunking and v2 vector store`；
4. 不创建远程仓库、不推送。

**为什么这样执行**

- **为什么要有 Git**：代码是"会演进的东西"。Git 记录每一次有意义的变更，
  让你能回看"这行代码为什么存在"、能回到任何历史版本。没有版本管理，
  一次误删就是灾难。
- **为什么先检查 .gitignore 再提交**：Git 提交的是"整个项目"，而 `.env`
  （含 API key）、数据库、缓存这些**不该进仓库**。`.gitignore` 就是"白名单
  之外的禁止清单"。第一次提交前花几分钟检查，比之后把密钥泄露出去
  （推送到远程）补救容易一万倍。
- **为什么扫描密钥**：API key 一旦进过 Git 历史，即使删掉也留在历史里。
  所以"提交前扫描"是硬规矩。
- **为什么一次提交只讲一件事**：commit 信息是给未来的人（包括你自己）
  看的。信息清楚的提交，回滚和排查都简单。

**执行结果**

- `.gitignore` 补齐（`.env`、两个向量库、缓存、虚拟环境、docs 全部忽略）；
- 21 个文件首次提交成功；工作区干净；
- 确认 .env 只含预期变量，README 中只有文档示例占位符，无真实密钥。

**0 基础解释**

- **git status / commit / log**：status 看"现在有什么变动"，commit 把变动
  存成历史快照，log 看历史。
- **为什么 .env 不能进 Git**：.env 里是密钥。仓库可能被分享、被推到云端，
  密钥一旦出去就等于把大门钥匙给了别人。
- **commit 不是备份**：commit 是"带说明的变更记录"，比备份多了一层
  "为什么"。

---

## 任务 6：混合检索实验（向量 + BM25）——结论：不接入

**执行内容**

对比三种检索方案，50 题评估：
- A：当前纯向量检索；
- B：纯 BM25 关键词检索；
- C：向量 + BM25 融合（RRF 融合和两种权重归一化融合）。

**为什么这样执行**

- **为什么要想混合**：前几轮发现"职位名称、人名"这类问题向量检索很弱。
  BM25 是"关键词精确匹配"——人名、专有名词它一抓一个准。直觉上"向量 +
  关键词"应该互补。
- **为什么做了两种融合方式**：RRF（排名融合）和加权归一化是两种主流做法，
  光做一种说服力不够。都跑出来，数据说话。
- **为什么"没有提升"也是好结论**：实验的价值不是"必须成功"，而是
  **用数据回答"该不该做"**。如果融合没有净收益，那"不接入"就是正确答案，
  省下的时间精力可以去解决真正的问题。**不是所有好主意都值得上线**。

**执行结果**

- 纯 BM25 在专名类确实更强（汇报对象 0/3 → 2/3），但语义类大幅变差；
- 所有融合方案都是"救 A 伤 B"：最好的变体 Top-1 只 +1 题、Top-3 还 -1 题；
- **结论：不接入**。BM25 的强项单独留作下一步实验的输入。

**0 基础解释**

- **向量检索**：把文字变成"数学向量"，按语义相似度找（"实习期"和
  "internship period"能互相找到，因为语义相近）。
- **BM25**：经典的关键词检索——查"Khor Kai Dat"就在文档里找这几个字。
  它不懂语义，但"精确匹配"这件事它做得比向量好。
- **RRF（Reciprocal Rank Fusion）**：把两个排序列表按"名次"合并打分，
  名次越靠前得分越高，最后合成一个新排名。
- **为什么两者难融合**：语料只有 15 块且每块很长，排名空间小；更重要的是
  "语义题"和"专名题"是两个不同的世界，加权天平往哪边偏都会牺牲另一边。

---

## 任务 7：Cross-Encoder 重排实验与正式接入

**执行内容**

1. 离线实验：A 纯向量（62%/80%）vs B 向量召回 Top-5 + Cross-Encoder 重排
   vs C 向量召回 Top-10 + 重排（82%/92%）；
2. 验证"0.85 阈值加入后结果无偏差"（等价性检查）；
3. 正式接入：新建 `reranker.py`（进程级单例 + lazy loading + 失败回退），
   `store.search` 改为"向量召回 Top-10 → 0.85 过滤 → 重排 → Top-k"；
4. /search 与 /query 共用同一检索函数，零 DeepSeek 额外费用。

**为什么这样执行**

- **为什么重排有效**：向量检索（双塔模型）为了快，把"问题和文档"分别编码，
  但这样会丢失两者之间的交互信息。Cross-Encoder 把"问题 + 候选段落"拼在
  一起同时编码，理解更精细——**先用快的模型粗筛（召回），再用慢但准的
  模型精排（重排）**，这就是经典的"召回-重排"两阶段架构。
- **为什么召回 Top-10 而不是 Top-5**：实验数据说话——Top-5 召回池太小，
  正确段落经常排在 5-10 名，重排再强也救不回来。Top-10 是实测最优。
- **为什么先做"阈值等价性检查"**：离线实验没加 0.85 阈值（正式流程有）。
  如果阈值会过滤掉正确答案，上线结果就和实验不一致。**上线前必须证明
  "正式环境 = 实验环境"**。检查结果：0 题偏差，才敢接入。
- **为什么单例 + lazy loading**：Cross-Encoder 模型加载要 10-30 秒、占内存。
  如果每次请求都加载一次，服务就废了。单例 = 全进程只加载一份；
  lazy = 第一次需要时才加载（启动快，首次请求慢，可接受）。
  并发时用锁保证不会重复加载。
- **为什么失败要回退而不是报错**：重排模型挂了，检索不能跟着挂——
  回退到纯向量排序，服务照常工作，只是"降级"而不是"宕机"。

**执行结果**

- Top-1 62% → **82%**，Top-3 80% → **92%**，混合题 3/10 → 8/10；
- 阈值等价性检查：加入 0.85 后与离线实验逐项一致；
- 成本：内存 +24MB、每题 +0.34 秒、启动加载一次约 10-30 秒；
- 真实 uvicorn 冒烟 12 项全过；199 个测试通过。

**0 基础解释**

- **双塔 vs 交叉编码器**：双塔 = "各自编码再比较"（快但粗）；
  交叉编码器 = "放一起看"（慢但细）。重排就是"先用双塔选出 10 个候选，
  再用交叉编码器把这 10 个排个精细的序"。
- **单例（singleton）**：整个程序只允许存在一个实例——模型只加载一次。
- **lazy loading（懒加载）**：用到才加载。服务启动时不加载模型，
  第一个请求触发加载。
- **降级（fallback）**：主方案失败时启用备胎方案，保证服务不死。

---

## 任务 8：候选补召回实验（Top-15 / BM25 补召回）——结论：不接入

**执行内容**

对比三种方案，50 题评估：
- A：当前正式方案（向量 Top-10 → 0.85 → 重排）；
- B：向量 Top-15 → 0.85 → 重排；
- C：向量 Top-10 ∪ BM25 Top-5 → 0.85 → 重排。
特别验证两题："Who does the intern report to?" 和 "实习生的直属上司是谁？"。

**为什么这样执行**

- **为什么想补召回**：上一轮剩下两题，其中"实习生的直属上司是谁？"的
  正确段落排在向量第 11 名——刚好在 Top-10 之外。直觉："把召回池扩到 15
  或者让 BM25 把第 11 名拉进来，不就救回来了吗？"
- **为什么必须解决"BM25 候选没有距离"的问题**：正式流程用 0.85 距离阈值
  过滤无关内容。BM25 只给出关键词分数，**没有向量距离**。方案：
  - 伪造一个距离？**绝对不行**——那等于假装它很相关；
  - 静默跳过过滤？**也不行**——那等于放开"无关内容保护"；
  - 正确答案：对 BM25 补进来的候选**用 embedding 真实计算距离**，
    再走同一套 0.85 过滤。不伪造、不绕过，规则对所有人一视同仁。
- **为什么"实验没提升"时还要保留脚本**：这是可复现性。结论是"不接入"，
  但证据（脚本、测试、数据）留在仓库里，将来想重新评估随时能跑。

**执行结果**

- B、C 与 A 完全一致（82%/92%），零命中改善；
- 根因清晰：BM25 补入的 26 个候选里 21 个真实距离 > 0.85 被过滤
  ——**不在向量 Top-10 的块，通常距离也超阈值**，补召回在可靠性规则下
  基本失效；
- 两题专项："Who does the intern report to?" 是重排层问题（候选在池内但
  CE 排低）；"实习生的直属上司是谁？" 需要查询改写而非补召回；
- 结论：不接入，正式系统保持 Top-10 + 0.85 + 重排；归档实验结果。

**0 基础解释**

- **为什么"距离超阈值"和"排名靠后"是一回事**：向量按距离排序，
  排名第 11 意味着它比前 10 个都"远"。0.85 阈值就是"远到不靠谱"的边界。
  所以"扩召回"和"守住质量"天然冲突——补回来的往往正是该被过滤的。
- **这条任务教会我们**：**不是所有直觉都值得上线；可靠性规则不能为
  "让某个用例通过"而打折**。宁可让一题答不出，也不能让系统开始
  "什么都敢答"。

---

## 任务 9：API 可靠性加固

**执行内容**

1. 上传限制：默认 10MB（`MAX_UPLOAD_BYTES` 可配置）、只支持 PDF/TXT/MD、
   空文件拒绝、路径穿越拒绝、临时文件用完即删；
2. DeepSeek 调用：连接/读取超时分开配置；对网络错误、超时、429、
   500/502/503/504 做**有限重试**（最多 3 次，退避 1s/2s）；
   400/401/403/404/422 不重试；
3. 错误码统一：不支持类型 400 → **415**；未知异常 500 只返回
   "服务器内部错误"（堆栈只进服务端日志）；
4. 每个请求生成 `request_id`，响应头返回，日志记录
   method/path/status/duration_ms；不记录请求体与密钥。

**为什么这样执行**

- **为什么上传要限制**：公网/多人用的服务，上传接口是最容易被攻击的入口。
  限制大小防资源耗尽；限制类型防奇怪文件；路径穿越检查防"用文件名偷
  服务端文件"；临时文件即用即删防磁盘堆积。
- **为什么 4xx 不重试**：重试的前提是"这次失败可能是暂时的"。
  429（限流）和 5xx（服务端故障）可能过一会就好；但 400（参数错）、
  401（密钥无效）**重试一万次结果都一样**，只会浪费时间和请求配额。
- **为什么"有限"重试**：无限重试 = 服务自己把自己拖死。3 次 + 退避
  （等 1 秒、2 秒）是"给上游喘气机会"又不至于无限等。
- **为什么 request_id**：一个请求穿过"客户端 → 服务 → DeepSeek"，
  出错时用户说"刚才失败了"，你靠什么定位？request_id 就是这条链路的
  追踪号——日志里一搜，这个请求发生了什么一目了然。
- **为什么日志不记 body**：请求体可能是文档全文（隐私）、密钥（致命）。
  日志只记"谁、在哪个接口、用了多久、结果如何"，不记"内容是什么"。
- **为什么未知异常不给客户端看堆栈**：堆栈会泄露服务器路径、内部结构，
  是攻击者的情报。客户端只需要知道"出错了"；详细的留给服务端日志。

**执行结果**

- 33 个可靠性测试全过，总计 243 个测试；
- 真实冒烟 13 项全过（10MB 上限 413、.exe 415、路径穿越 400、空文件 400、
  /search、/query、无关问题、request_id 头）；
- 结构化日志实测输出正常。

**0 基础解释**

- **幂等 vs 重试**：能安全重试的操作叫"可重试"。读操作重试没问题；
  写操作要小心。这里的重试都是"再调一次模型接口"，安全。
- **状态码速查**：400 请求本身不对；401/403 没权限；413 太大；415 类型
  不对；422 参数校验不过；429 太频繁；5xx 服务器那边出问题。
- **脱敏（redaction）**：把敏感信息从日志/错误消息里去掉。规矩：
  **日志可以详细，但永远不能有密钥和用户内容**。

---

## 任务 10：Docker 本地部署配置

**执行内容**

新增 `Dockerfile`、`.dockerignore`、`docker-compose.yml` 和 19 项配置
静态检查测试；README 补充 PowerShell 部署命令。**本机没有安装 Docker，
本轮只完成了配置文件和静态检查，没有做真实容器构建与运行验证**——这一
点明确写进了 README。

**为什么这样执行**

- **为什么用 slim 镜像 + CPU torch**：镜像体积直接决定拉取和启动时间。
  python:3.12-slim 是官方精简版；torch 装 CPU 版而不是 CUDA 版，
  能省下 2GB+——本地部署不需要 GPU。
- **为什么 API key 不写进镜像**：镜像是"可复制、可分享"的。密钥进镜像 =
  密钥跟着镜像到处跑。正确做法：运行时通过环境变量或 `env_file` 注入。
- **为什么端口只绑 127.0.0.1**：`8000:8000` 会暴露到局域网/公网；
  `127.0.0.1:8000:8000` 只有本机能访问。本地部署就该这样。
- **为什么向量库要挂卷**：容器是"临时"的（重建就没了）。把
  `chroma_data_v2` 挂成持久卷，容器重启/重建后数据还在。
- **为什么模型缓存也挂卷**：模型下载一次 ~170MB，挂卷后重建镜像不用
  重新下载。
- **为什么做静态检查而不是假装验证**：**没有 Docker 就是没有 Docker**，
  不能说"应该能跑"。配置文件可以被静态检查（指令、排除清单、yaml 结构），
  但"能构建、能启动、能查询"必须等 Docker 可用后真实验证。
  这是诚实工程：**文档里写清楚"未验证"，比假装成功有价值得多**。

**执行结果**

- 4 个配置文件 + 19 项静态检查测试，总计 262 个测试通过；
- README 含 PowerShell 构建/启动/日志/停止/故障排查命令；
- 明确声明：真实容器构建与运行验证待 Docker 可用后进行。

**0 基础解释**

- **镜像 vs 容器**：镜像 = 打包好的"安装盘"（只读）；容器 = 用安装盘
  启动的一个运行实例（可读写、可删）。删容器不影响镜像。
- **卷（volume）**：容器外的一块持久存储，专门用来放"容器没了也不能丢"
  的数据。
- **healthcheck（健康检查）**：定时访问 /health 确认服务活着，
  不健康就自动重启。
- **这轮的心法**：**环境没有的能力，绝不假装有**。工具没装就如实说，
  把能做的（配置 + 检查）做到位，把不能做的（真实验证）写清楚。

---

## 写在最后：贯穿始终的几条心法

1. **先证明，再上线**：切块、重排、混合检索……每一步都是先离线实验、
   数据说话，再动正式环境。
2. **可回滚**：旧库永远保留，切换永远有退路。
3. **不伪造、不绕过**：BM25 候选的距离是真实算的；可靠性规则对所有
   候选一视同仁；Docker 没验证就明说。
4. **测试是安全网**：从 79 个测试长到 262 个，每次改动都有自动化检查兜底。
5. **数据比直觉重要**："我觉得该补召回" → 实验 → 数据说"不" →
   那就不做，并留下证据。
