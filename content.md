# Content：这个项目做了什么，以及为什么

> 本文讲的是**思路和逻辑**：这个系统是什么、每个部件解决什么问题、
> 每个决策背后的推理链、以及整体是怎么一步步搭起来的。
> 想按任务逐个看"为什么这么执行"，请读 `ai.md`。

---

## 一、这个项目是什么

一句话：**上传文档建知识库，然后用 AI 回答关于这些文档的问题，
答案必须带出处（"哪个文件第几页"）。**

它解决的问题：让"问文档"变成可能。比如公司有一堆规章制度 PDF，
员工想知道"实习期多久"，不用翻文件，直接问系统，系统从文档里找到
答案并告诉你出处——**答案不是 AI 编的，是从你的文档里检索出来的**。

技术上这叫 **RAG（Retrieval-Augmented Generation，检索增强生成）**：
先"检索"（从文档里找出相关段落），再"生成"（让大模型基于这些段落写答案）。

---

## 二、核心概念（零基础版）

| 概念 | 是什么 | 打个比方 |
|---|---|---|
| **chunk（块）** | 文档被切成的一段段文本 | 把一本书拆成一张张卡片 |
| **embedding（向量化）** | 把文本变成一串数字（向量），语义相近的文本数字也相近 | 给每张卡片编号，内容像的编号也接近 |
| **向量检索** | 拿问题的向量去库里找最接近的卡片 | 拿问题去"卡片库"里按语义找最像的几张 |
| **0.85 阈值** | 距离超过 0.85 的卡片视为"太不相关，别给模型看" | 只递"看着靠谱"的卡片 |
| **重排（rerank）** | 先用快而粗的方法选 10 张，再用慢而精的方法排次序 | 先海选，再精评 |
| **RAG** | 检索 + 生成两步走 | 先查资料，再让 AI 照着资料写回答 |
| **改写（rewrite）** | 中文问题先翻译成英文再去检索（中文检索效果差） | 翻译官 |

---

## 三、系统长什么样（架构）

```
用户提问
   │
   ├─ 中文/混合问题 ──→ [DeepSeek] 改写成英文检索查询（零费用原则之外的唯一额外调用）
   │                     （英文问题跳过此步）
   │
   ├─ 向量召回 Top-10（all-MiniLM-L6-v2，本地，免费）
   │     └─ 0.85 距离阈值过滤（无可靠结果 → 返回空，绝不硬答）
   │           └─ Cross-Encoder 重排（本地，免费）
   │                 └─ 取前 Top-k
   │
   ├─ /search：直接返回段落数组（零 DeepSeek 调用）
   └─ /query：  [DeepSeek] 用原始问题生成带出处的答案
```

**钱花在哪**：DeepSeek 只在两个地方被调用——① 中文问题的改写、
② 最终答案生成。检索、向量化、重排全部本地免费。

---

## 四、我们做了什么（时间线）

| 阶段 | 做了什么 | 结果 |
|---|---|---|
| 1 | 修复"英文问题返回中文" | 提示词强制分语言规则 |
| 2 | 切块策略实验（A/B/C） | 选定方案 C（段落+重叠），Top-3 68%→80% |
| 3 | 方案 C 正式化 + 候选库 v2 | 15 块候选库，评估完全复现 |
| 4 | 正式切换方案 C + v2 库 | 上传流程与正式库全部切换，旧库留作回滚 |
| 5 | Git 初始化 + 首次提交 | 版本管理 + 密钥防护（.gitignore） |
| 6 | 混合检索实验（BM25 融合） | 结论：不接入（无净收益） |
| 7 | Cross-Encoder 重排实验 + 接入 | **Top-1 62%→82%，Top-3 80%→92%** |
| 8 | 补召回实验（Top-15/BM25） | 结论：不接入（可靠性过滤下失效） |
| 9 | API 可靠性加固 | 10MB 上限、有限重试、request_id、脱敏 |
| 10 | Docker 部署配置 | 配置文件 + 静态检查（本机无 Docker 未实跑） |

---

## 五、每个关键决策，为什么这么做

### 1. 为什么切块用"自然段落 + 相邻段重叠"（方案 C）

**问题**：旧策略把整页文字压成一行，按 500 字符的窗口硬切。窗口是"字符
尺子"，不是"语义尺子"——一个答案可能被劈成两半，或一块里塞满无关内容。

**方案 C 的思路**：
- **按自然段落切**：段落是文档自己的语义边界（一个段落讲一件事）。
- **相邻块共享下一段**（重叠）：防止答案恰好卡在两块接缝处。
  段 A+段 B 是一块，段 B+段 C 是另一块，段 B 在两边都出现——
  检索只要碰到段 B 就能找到。

**为什么不用更大/更小的块**：切块实验用 50 题实测——方案 C 的
Top-1 62%、Top-3 80%，全面优于旧策略（56%/68%）。**结论来自数据，
不是感觉。**

### 2. 为什么数据库用"候选库"迁移而不是直接覆盖

换切块策略 = 全部文档要重新切、重新向量化。正确顺序：

```
建 chroma_data_v2（新库）→ 评估复现 → 真实冒烟 → 切换 DB_DIR → 重启
                                ↑ 旧库 chroma_data 全程不动，作为回滚库
```

任何时候发现不对，把 `DB_DIR` 改回去重启即可。**"换"永远有退路。**

### 3. 为什么检索要"召回 + 重排"两阶段

- **向量检索（召回）**：快，但"粗"——问题和文档分开编码，理解不了
  两者之间的精细关系；
- **Cross-Encoder（重排）**：慢，但"细"——把问题和每个候选拼在一起
  同时理解。

于是：**先用快的选 10 个（召回），再用慢的排好序（重排）**。
实测：Top-1 从 62% 提到 82%，混合题从 3/10 提到 8/10，
代价只是每题 +0.34 秒和 +24MB 内存。

**为什么是 Top-10 而不是 Top-5**：实验发现正确答案经常排在向量第 5-10 名，
Top-5 召回池装不下——召回池太小，重排再强也白搭。

### 4. 为什么 0.85 阈值是"铁律"

距离 > 0.85 的段落 = "跟问题远到不靠谱"。这个阈值保证：

- 检索不到可靠内容时，/search 返回空数组、/query 说"资料中找不到"，
  **而不是让模型瞎编**；
- 任何候选（包括 BM25 补进来的）都要过这一关——**重排可以改变顺序，
  但不能把"无关"变"有关"**。

补召回实验正是被这条铁律拦住的：BM25 补进的 26 个候选，21 个真实距离
超阈值被过滤——不是实现问题，是数据本质：**排在向量 Top-10 之外的内容，
通常本来就远**。这轮实验的结论是"不接入"，并且这个结论是对的。

### 5. 为什么重排模型要"单例 + 懒加载 + 失败回退"

- **单例**：模型加载要 10-30 秒，全进程只加载一份，后续请求复用；
- **懒加载**：服务启动时不加载，第一个请求才触发（启动快）；
- **并发锁**：多个请求同时到达时，只有第一个真正加载，其余等待复用；
- **失败回退**：模型挂了 → 记一条日志 → 退回纯向量排序。
  检索"降级"而不是"宕机"。

### 6. 为什么 DeepSeek 调用要"有限重试"

- **可重试的**：网络抖动、超时、429（限流）、500/502/503/504（服务端
  临时故障）——这些"过一会儿可能就好"，最多重试 3 次、退避等待；
- **不可重试的**：400（参数错）、401/403（密钥无效）——重试一万次
  结果一样，立即失败，不浪费请求。

### 7. 为什么要有 request_id 和脱敏日志

一个请求要经过"客户端 → 服务 → DeepSeek"，出错时没有追踪号就无从查起。
`request_id` 让每一条日志都能串成一条线。同时日志**只记元信息**
（方法/路径/状态码/耗时），**不记请求体**（可能是文档全文）和密钥——
详细归详细，敏感信息永不落日志。

### 8. 为什么 API key 永远不进镜像/仓库

- Git 历史里的密钥删不掉（历史永久保留）；
- 镜像会被复制和分享，密钥进镜像 = 密钥满世界跑；
- 所以：.env 被 .gitignore 和 .dockerignore 双重排除，容器里通过
  `env_file` 在运行时注入。

### 9. 为什么这轮 Docker 只做静态检查

本机没有安装 Docker。可以写配置文件、可以写 19 项静态检查测试，
但**不能假装构建和运行成功**。README 里明确写"未经真实验证"——
诚实声明"没做过"，是让结论可信的前提。

---

## 六、怎么做的：关键实现细节

### 数据流

1. **上传**（/documents/upload）：流式接收 → 校验扩展名（415）→
   校验大小（413）→ 安全文件名（防路径穿越）→ 临时文件 → 方案 C 切块 →
   先删旧数据再入库（upsert）→ 清理临时文件。
2. **入库**（store.py）：chunk 带 `source`（文件名）和 `page`（页码）
   两个元数据字段，写入 `chroma_data_v2/`。
3. **检索**（store.search，/search 与 /query 共用）：
   `向量召回 Top-10 → 0.85 过滤 → Cross-Encoder 重排 → Top-k`。
   返回的 `distance` 永远是**原始向量距离**（重排只改顺序不改字段）。
4. **问答**（/query）：中文问题先改写为英文检索查询 → 检索 →
   用**原始中文问题**生成答案（语言跟随提问语言）。

### 切块（方案 C）的规则

- 按空行识别自然段落，段落内空白压成单空格；
- 每块 = 当前段 + 下一段（滑动窗口，步长 1 段）；
- 相邻段**不跨 PDF 页面**合并（page 元数据必须准确）；
- 超长段落内部按句子断点再切（不产生空块）；
- 连续完全相同的段落预合并（不产生重复块）；
- 每个块保留 source / page。

### 可靠性规则

- 上传：10MB 默认（`MAX_UPLOAD_BYTES` 可配置）、只支持 .pdf/.txt/.md、
  空文件拒绝、路径穿越拒绝；
- DeepSeek：连接 10s / 读取 60s 超时；3 次有限重试 + 退避；
- 错误码：400 参数/路径穿越、413 过大、415 类型不支持、422 校验失败、
  502 上游认证/服务/连接异常、503 限流、500 未知异常（不暴露堆栈）；
- 日志：`request_id method path status duration_ms`，响应头 `X-Request-ID`。

### Docker（已配置，未实跑）

- 镜像：python:3.12-slim + CPU torch + 非 root 用户 + uvicorn 8000；
- compose：env_file 传 key、`127.0.0.1:8000:8000`（仅本机）、
  `chroma_data_v2` 持久卷、healthcheck 探测 /health、HF 缓存卷。

---

## 七、当前状态

| 项目 | 状态 |
|---|---|
| 检索链路 | 向量 Top-10 → 0.85 → Cross-Encoder 重排（正式） |
| 切块 | 方案 C（段落 + 相邻段重叠，正式） |
| 正式库 | `chroma_data_v2/`（15 块）；回滚库 `chroma_data/`（10 块） |
| 测试 | 262 个全部通过 |
| Git | 本地仓库，6 个 commit，未推送 |
| Docker | 配置完成 + 静态检查通过，真实容器验证待 Docker 可用后进行 |

**回滚路径**：`store.py` 的 `DB_DIR` 改回 `chroma_data` 并重启，即回到
旧切块 + 旧库；上传入口如需回退，把 `load_document_paragraphs` 换回
`load_document`。

---

## 八、已知边界（诚实清单）

1. **专名/职位类问题**（"Who does the intern report to?" 等 6 题）：
   当前 embedding 模型是词级理解，对人名、职位这类"词面零重合"的问题
   命中有限——重排已救回部分，剩余需要查询改写增强，是下一步方向；
2. **"实习生的直属上司是谁？"**：正确段落排在向量第 11 名，Top-10 召回
   外 + 重排未救回，补召回实验证明可靠性规则下无解，需组合手段；
3. **Docker 未真实验证**：配置文件与静态检查完成，待本机安装 Docker 后
   按 README 命令实跑；
4. **模型行为偶发波动**：答案语言/引用格式在多次采样中 99% 稳定，
   但大模型输出本质是概率性的，极端情况下可能有一次不合规输出。

---

## 九、如果重新做一遍，哪些决策不变

1. **先实验、后上线**——切块和重排的收益都是先被数据证明的；
2. **可回滚**——v2 迁移全程旧库不动；
3. **可靠性规则不打折**——0.85 阈值、4xx 不重试、密钥不进仓库/镜像；
4. **不伪造**——BM25 距离真实计算、Docker 未验证就明说；
5. **测试跟着功能长**——从 79 到 262 个测试，每次行为变化都有自动检查。
