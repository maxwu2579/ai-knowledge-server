chromadb>=1.0
# sentence-transformers 同时提供 bi-encoder（向量检索）与 CrossEncoder（重排，
# 模型 cross-encoder/ms-marco-MiniLM-L-6-v2，只推理，缓存于用户目录）
sentence-transformers>=3.0
pypdf>=5.0
httpx>=0.27
fastapi>=0.115
python-multipart>=0.0.20
uvicorn>=0.52
pytest>=8.0
